import 'package:flutter/material.dart';
import 'package:renttt/utils.dart/tab_view.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TabView();
  }
}
